[project]
name = "trip-planner-backend"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "pydantic>=2.0",
    "uvicorn[standard]",
    "anthropic",
    "haversine>=2.9.0",
]

[tool.uv]
package = false
